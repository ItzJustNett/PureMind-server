# IMPORTANT: Install PyTorch CPU-only FIRST to avoid 4GB+ CUDA downloads:
# pip3 install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cpu
# Then install this file:
# pip3 install --no-cache-dir -r requirements.txt

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Speech services
gtts==2.3.2
transformers==4.34.0
librosa==0.10.1

# Core dependencies
numpy>=1.26.0
requests==2.28.2
httpx==0.25.0
aiofiles==23.2.1

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# CLI tools (optional - only for nicer-cli-client.py)
# Uncomment if you need the CLI client:
# rich==13.0.0
# questionary==1.10.0
